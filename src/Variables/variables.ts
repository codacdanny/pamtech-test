type Colors = {
  primaryColor: string;
  bgColor: string;
  campaignTextColor: string;
  textColor: string;
  headerColor: string;
  buttonBgColor: string;
  faintTextColor: string;
  navButtonBg: string;
};
export const colors: Colors = {
  primaryColor: "#FFFFFF",
  bgColor: "#CCCFCE3D",
  campaignTextColor: "#55605C",
  textColor: "#808785",
  headerColor: "#00100B",
  buttonBgColor: "#004741",
  faintTextColor: "#A6A6A5",
  navButtonBg: "#F3F4F6",
};
